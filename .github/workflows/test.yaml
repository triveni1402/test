name: test

on:
  workflow_dispatch

jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello World"
  
  job2:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hi there"

  job3:
    runs-on: ubuntu-latest
    needs:
        - job1
        - job2
    steps:
       -  run: echo "Welcome to GitHub Actions"
  